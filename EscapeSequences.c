//Write a program to Escape Sequence in C with RitikCoder.
#include"stdio.h"
#include"conio.h"
int main(){
    printf("Escape Sequences in C programming\n");
    /*An Escape sequence in C programming language is a sequence of characters.
    and It donesn't represent itself when used inside string literal or character.
    and it is also composed of two or more characters starting with backslash (\).
    for example:
    1 -> \n represent new line.
    2 -> \t represent a tab space.
    3 -> \a represent weep sound of windows.
    4 -> \ represent \, ", ' and so on...
    and so on...
    */
    printf("1. \\n is used to new line in C\n");
    printf("2. \t\\t is used to tab space in C\n");
    printf("3. \\a is used to weep sound of windows in C\n");
    printf("4. \\ is used to show \\, \", \' and so on... in C\n");
    printf("and so on....\n");
    //These are the mainly used Escape sequence characers in C programming.

    //getch();
    return(0);
}