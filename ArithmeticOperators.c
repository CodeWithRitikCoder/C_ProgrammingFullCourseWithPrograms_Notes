//Write a program to use Arithmei operator in C with RitikCoder.
#include"stdio.h"
#include"conio.h"
int main(){
    printf("Arithmeic Operator in C programming\n");
    int a= 10, b= 6;
    //Arithmetic operators are :- (+, -, *, /, %).
    printf("1. Addition of A + B = %d\n", (a+ b));
    printf("2. Subtraction of A - B = %d\n", (a- b));
    printf("3. Multiplication of A * B = %d\n", (a* b));
    printf("4. Division of A / B = %d\n", (a/ b));
    printf("5. Modulus of A %% B = %d\n", (a% b));
    //These all are my Arithmetic operators in C programming.
    
    //getch();
    return(0);
}