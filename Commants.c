//Write a program to Understand commands in C with RitikCoder.
#include"stdio.h"
#include"conio.h"
int main(){
    printf("Commands in C programming\n");
    /*There are two way to command our code or own things.
    1. -> // - double forword slash is used to single line command in C.
    2. -> forword slash* things to command *forword slash - is used to multi line command in C.
    as I used already for this whole paragraph.
    */
    printf("// -> This is used to single line command.\n");
    printf("/* things to command */ -> This is used to multi line command.\n");
    
    //getch();
    return(0);
}