//Write a program to user all Logical Operators in C with RitikCoder.
#include"stdio.h"
#include"conio.h"
int main(){
    printf("Logical Operators in C programming\n");
    int a= 0, b= 1;
    //Logical operators are :- (&&, ||, !).
    printf("1. Is A && B : %d\n", (a && b));
    printf("2. Is A && B : %d\n", (a || b));
    printf("3. Is A && B : %d\n", !(a && b));
    printf("4. Is A && B : %d\n", !(a || b));
    //These all are my Logical operators in C programming.
    
    //getch();
    return(0);
}